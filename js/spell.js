class Spell {
  constructor(name) {
    this.name = name || '';
    this.description;
    this.repeat = 1;
  }

  effect(encounter) { // We will define the effect function for each spell depending on what they do

  }
}